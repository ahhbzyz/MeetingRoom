<?xml version="1.0" encoding="utf-8"?>
<manifest package="com.badoo.meetingroom"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.MANAGE_ACCOUNTS" />
    <uses-permission android:name="android.permission.USE_CREDENTIALS" />

    <application
        android:name=".di.AndroidApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".presentation.view.activity.GetCredentialActivity"
            android:label="Meeting Room">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".presentation.view.activity.RoomStatusActivity"
            android:windowSoftInputMode="adjustNothing"/>
        <activity android:name=".presentation.view.activity.EventsCalendarActivity" />
        <activity android:name=".presentation.view.activity.RoomBookingActivity"/>
        <activity android:name=".presentation.view.activity.AllRoomsActivity"/>
    </application>
</manifest>